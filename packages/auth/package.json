{
  "name": "@workspace/auth",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "env:load": "dotenv -e ../../.env --",
    "auth:generate": "pnpm env:load pnpm dlx @better-auth/cli generate --output ../db/src/schema/auth.ts --y",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@better-auth/stripe": "^1.3.4",
    "@workspace/db": "workspace:*",
    "@workspace/email": "workspace:*",
    "@workspace/env": "workspace:*",
    "bcryptjs": "^3.0.2",
    "better-auth": "^1.3.4",
    "stripe": "^18.4.0"
  },
  "devDependencies": {
    "@workspace/tsconfig": "workspace:*",
    "typescript": "^5.7.3"
  },
  "exports": {
    ".": "./src/index.ts",
    "./constants": "./src/constants.ts",
    "./client": "./src/client.ts"
  }
}